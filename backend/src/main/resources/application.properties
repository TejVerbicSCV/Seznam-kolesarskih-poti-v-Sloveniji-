spring.application.name=projektna-naloga

# CockroachDB connection - using separate properties (recommended for Spring Boot)
# This matches your Node.js connection string but uses Spring Boot property format
spring.datasource.url=jdbc:postgresql://last-baboon-11742.jxf.gcp-europe-west3.cockroachlabs.cloud:26257/projektna_naloga?sslmode=require
spring.datasource.username=tej
# IMPORTANT: Replace this with your actual password from CockroachDB
# If password contains special characters, Spring Boot should handle them automatically
spring.datasource.password=YaaTDYNzU1dGUsP9bVzjFw
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

# JPA nastavitve
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
server.error.include-exception=true
